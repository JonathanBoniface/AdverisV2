{{ form_start(form) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.start_date) }}</div>

</div>
<div class="row">

    <div class="col-md-2">{{ form_row(form.name) }}</div>
    <div class="col-md-2">{{ form_row(form.price_sold) }}</div>
</div>
<div class="row">
    <div class="col-md-2">{{ form_row(form.estimated_time) }}</div>
    <div class="col-md-2">{{ form_row(form.spent_time) }}</div>
    <div class="col-md-2">{{ form_row(form.technology) }}</div>
</div>
<div class="row">
    <div class="col-md-2">{{ form_row(form.type) }}</div>
    <div class="col-md-2">{{ form_row(form.companies) }}</div>
    <div class="col-md-2">{{ form_row(form.workers) }}</div>
</div>

    {{ form_widget(form)}}

<input type="submit" class="btn btn-primary" value="Enregistrer">

{{ form_end(form) }}